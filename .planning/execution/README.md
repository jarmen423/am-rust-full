# Port gap execution registry

Canonical wave artifacts live in the parent monorepo:

`D:\code\agentic-memory\.planning\execution-am-rust-full\`

- `PORT_GAP_PLAN.md` — feature classifications (port / adapt / defer / out-of-crate)
- `DRAWING_SPIKE.md` — native vs Excalidraw bridge decision
- `OBSERVABILITY_CONTRACT.md` — workflows, attempts, diagnostics
- `ROADMAP.md`, `tasks.json`, `handoffs/w*/HANDOFF.md`

Do not duplicate those files here; update the canonical path when wave status changes.
